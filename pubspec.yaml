name: doctro
description: A new Flutter project.
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.2+5

environment:
  # Flutter 3.27 is the floor this project targets; Flutter 3.27 ships Dart 3.6,
  # so the Dart floor follows from it. The upper bound is capped at 4.0.0 because
  # Dart 4 is a breaking major release, and the previous `<6.0.0` would have
  # silently accepted it.
  sdk: '>=3.6.0 <4.0.0'
  flutter: '>=3.27.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  hugeicons: ^1.2.0
  flutter_localizations:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2
  flutter_svg: ^2.0.7
  progress_indicators: ^1.0.0
  flutter_rating_bar: ^4.0.1
  retrofit: ^4.0.1
  country_picker: ^2.0.19
  shared_preferences: ^2.0.15
#  fluttertoast: ^8.1.2
  fluttertoast: ^8.2.12
#  pinput: ^3.0.1
  pinput: ^5.0.1
  cached_network_image: ^3.2.3
  
  dropdown_plus: ^0.0.9
  #   git:
  #     url: https://github.com/thirstyde/flutter_paystack_rahul.git
  url_launcher: ^6.1.7
  # onesignal_flutter: ^5.2.7
#  onesignal_flutter: ^5.3.0

  
  pdf: ^3.8.4
  # path_provider: ^2.0.11
  path_provider: ^2.1.5


  provider: ^6.0.5
  #  syncfusion_flutter_pdfviewer: ^20.4.38
  syncfusion_flutter_pdfviewer: ^28.1.33
  syncfusion_flutter_core: ^28.1.33
  intl: ^0.20.2
  http_auth: ^1.0.1
  #  flutter_pdfview: ^1.3.1
  # connectivity: ^3.0.6
  full_screen_image_null_safe: ^2.0.0
  # firebase_core: ^2.4.0
  # firebase_core: ^2.32.0
  firebase_core: ^3.12.1

  # firebase_auth: ^4.2.3
  # firebase_auth: ^4.16.0
  firebase_auth: ^5.5.1

  google_sign_in: ^6.3.0
  firebase_core_platform_interface: ^6.0.0
  # cloud_firestore: ^4.3.0
  cloud_firestore: ^5.6.5
  # firebase_storage: ^11.6.5
  firebase_storage: ^12.4.4
  # firebase_messaging: ^14.2.0
  firebase_messaging: ^15.2.4
  # flutter_local_notifications: ^15.1.1
  flutter_local_notifications: ^18.0.1

  photo_view: ^0.15.0
  # webview_flutter: ^3.0.4  
  webview_flutter: ^4.10.0

  uuid: ^4.1.0
  http: ^1.0.0
  connectivity_plus: ^6.1.3
  # image_picker: ^1.0.7
  image_picker: ^1.0.2
  flutter_launcher_icons: ^0.14.4
  logger: ^2.6.0
  dio: ^5.9.0
  http_parser: ^4.1.2
  agora_rtc_engine: ^6.5.3
  record: ^6.2.0
  audioplayers: ^6.1.0
  supabase_flutter: ^2.8.3
  camera: ^0.11.0
  google_mlkit_face_detection: ^0.11.0
  path: ^1.9.0
  local_auth: ^2.3.0
  flutter_secure_storage: ^9.2.2
  flutter_dotenv: ^5.2.1
  hive_flutter: ^1.1.0
  firebase_crashlytics: ^4.2.4
  flutter_animate: ^4.5.2
  shimmer: ^3.0.0
  minio: ^3.0.2
  flutter_markdown_plus: ^1.0.12
dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  retrofit_generator: ^9.0.0
  build_runner: ^2.3.3
  pip_view: ^0.9.7
  permission_handler: ^11.0.0
  # pip_view: ^0.9.7
  change_app_package_name: ^1.1.0
  flutter_lints: ^5.0.0


flutter_icons:
   image_path: "assets/images/appIcon.png"
   android: true
   ios: true


  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/icons/
    - assets/sounds/
    - lib/core/localization/language/
    
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # Uni Neue (licensed, purchased family - see assets/fonts/UniNeue/).
  # Weights are the usWeightClass values embedded in each file (checked with
  # fonttools), not a guess from the file name: Thin=250->100, Light=300,
  # Book=400, Regular=500, Bold=700, Heavy=800, Black=900. AyurezeTheme.font()
  # only ever asks for w500/w700/w800, so Regular/Bold/Heavy cover every style
  # the app actually uses; the rest are here for completeness.
  fonts:
    - family: Uni Neue
      fonts:
        - asset: assets/fonts/UniNeue/UniNeue-Thin.ttf
          weight: 100
        - asset: assets/fonts/UniNeue/UniNeue-ThinItalic.ttf
          weight: 100
          style: italic
        - asset: assets/fonts/UniNeue/UniNeue-Light.ttf
          weight: 300
        - asset: assets/fonts/UniNeue/UniNeue-LightItalic.ttf
          weight: 300
          style: italic
        - asset: assets/fonts/UniNeue/UniNeue-Book.ttf
          weight: 400
        - asset: assets/fonts/UniNeue/UniNeue-BookItalic.ttf
          weight: 400
          style: italic
        - asset: assets/fonts/UniNeue/UniNeue-Regular.ttf
          weight: 500
        - asset: assets/fonts/UniNeue/UniNeue-RegularItalic.ttf
          weight: 500
          style: italic
        - asset: assets/fonts/UniNeue/UniNeue-Bold.ttf
          weight: 700
        - asset: assets/fonts/UniNeue/UniNeue-BoldItalic.ttf
          weight: 700
          style: italic
        - asset: assets/fonts/UniNeue/UniNeue-Heavy.ttf
          weight: 800
        - asset: assets/fonts/UniNeue/UniNeue-HeavyItalic.ttf
          weight: 800
          style: italic
        - asset: assets/fonts/UniNeue/UniNeue-Black.ttf
          weight: 900
        - asset: assets/fonts/UniNeue/UniNeue-BlackItalic.ttf
          weight: 900
          style: italic
