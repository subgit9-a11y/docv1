gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java

# Remember to never publicly share your keystore.
# See https://flutter.dev/docs/deployment/android#reference-the-keystore-from-the-app
key.properties
**/*.keystore
**/*.jks

# app/debug.keystore is the exception: it is a fixed, non-production debug key
# checked in deliberately so every build (local and CI) signs with the same
# certificate. See the signingConfigs comment in app/build.gradle.
!app/debug.keystore
